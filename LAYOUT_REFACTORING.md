# 🔄 Рефакторинг структуры layout

## Что было сделано:

Вынесена общая часть (head и header) в отдельные шаблоны для упрощения настройки шапки сайта.

## Структура:

### 1. Публичные страницы (все кроме админки)
**Файл:** `resources/views/layouts/partials/public-header.blade.php`

Содержит:
- `<head>` секцию с мета-тегами, SEO, стилями
- Яндекс.Метрику
- `<body>` открывающий тег
- Компонент developer-contacts
- `<header>` с логотипом и навигацией

### 2. Админ-панель
**Файл:** `resources/views/layouts/partials/admin-head.blade.php`

Содержит:
- Упрощенный `<head>` без Яндекс.Метрики
- Базовые стили
- `<body>` открывающий тег
- Без header (навигация админки в `admin.partials.navigation`)

### 3. Основной layout
**Файл:** `resources/views/layouts/app.blade.php`

Теперь использует:
- `public-header` для всех страниц кроме админки
- `admin-head` для страниц админки
- Общий `<main>`, `<footer>` и `@stack('scripts')`

## Преимущества:

1. ✅ **Упрощенная настройка шапки** - все изменения в одном файле `public-header.blade.php`
2. ✅ **Разделение логики** - публичные страницы и админка имеют разные шапки
3. ✅ **Легче поддерживать** - не нужно искать код по всему layout файлу
4. ✅ **Чище код** - основной layout стал короче и понятнее

## Как изменить шапку:

### Для публичных страниц:
Редактируйте файл: `resources/views/layouts/partials/public-header.blade.php`

Здесь можно изменить:
- Логотип
- Навигационное меню
- Стили header
- Яндекс.Метрику
- SEO мета-теги

### Для админки:
Редактируйте файл: `resources/views/layouts/partials/admin-head.blade.php`

## Файлы:

- `resources/views/layouts/app.blade.php` - основной layout
- `resources/views/layouts/partials/public-header.blade.php` - head + header для публичных страниц
- `resources/views/layouts/partials/admin-head.blade.php` - head для админки
