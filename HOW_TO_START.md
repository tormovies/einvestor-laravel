# 🚀 Как запустить сервер

## ⚠️ ВАЖНО: `php artisan serve` НЕ РАБОТАЕТ на Windows!

**Используйте встроенный PHP сервер напрямую.**

**Самый быстрый способ:** Дважды кликните на **`START_SERVER_DIRECT.bat`**

Или смотрите подробную инструкцию: **`SERVER_START_INSTRUCTIONS.md`**

---

## ✅ Способ 1: Ручной запуск (РЕКОМЕНДУЕТСЯ)

1. **Откройте PowerShell или CMD**

2. **Перейдите в папку проекта:**
   ```bash
   cd c:\projects\einvestor-laravel
   ```

3. **Запустите сервер:**
   ```bash
   php artisan serve --port=8000
   ```

4. **Дождитесь сообщения:**
   ```
   INFO  Server running on [http://127.0.0.1:8000]
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:8000
   ```

---

## ✅ Способ 2: Через batch файл

1. **Дважды кликните на:** `START_SERVER.bat`

2. **Откроется окно CMD с сервером**

3. **Не закрывайте это окно!** Сервер работает, пока окно открыто.

---

## ✅ Способ 3: Через PowerShell скрипт

1. **Откройте PowerShell**

2. **Перейдите в папку проекта:**
   ```powershell
   cd c:\projects\einvestor-laravel
   ```

3. **Запустите скрипт:**
   ```powershell
   .\START_SERVER.ps1
   ```

---

## ❌ Если порт занят

Если порт 8000 занят, используйте другой:

```bash
php artisan serve --port=8001
```

Или освободите порт:

```powershell
# Найти процесс на порту 8000
netstat -ano | findstr ":8000"

# Остановить процесс (замените PID на реальный ID)
taskkill /PID <PID> /F
```

---

## 🔍 Проверка работы сервера

После запуска проверьте:

1. **В терминале должно быть:**
   ```
   INFO  Server running on [http://127.0.0.1:8000]
   ```

2. **Проверка в браузере:**
   ```
   http://localhost:8000
   ```

3. **Проверка порта:**
   ```powershell
   netstat -ano | findstr ":8000"
   ```

---

## ⚠️ Важно

- **Не закрывайте окно терминала** пока сервер работает
- Сервер останавливается по `Ctrl+C` или при закрытии окна
- Если нужно, чтобы сервер работал постоянно, используйте процесс-менеджер

---

## 📋 Доступные страницы после запуска

- Главная: http://localhost:8000/
- Статьи: http://localhost:8000/articles
- Товары: http://localhost:8000/products
- Категории: http://localhost:8000/category/{slug}
- Теги: http://localhost:8000/tag/{slug}

---

**Если ничего не помогает:** Запустите сервер вручную через командную строку и посмотрите на ошибки, которые появятся в терминале.
