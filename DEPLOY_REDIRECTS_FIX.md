# 🚀 Деплой исправления редиректов

**Изменения:** Исправлен `RedirectMiddleware` для работы редиректов с путями, начинающимися с `articles`, `products` и т.д.

## Команды для деплоя на продакшен

**Сервер:** `adminfeg@narnia`  
**Путь:** `~/einvestor.ru/laravel`  
**PHP:** `php8.3`

### Вариант 1: Пошаговый деплой (рекомендуется)

```bash
# 1. Подключение к серверу
ssh adminfeg@narnia

# 2. Переход в директорию проекта
cd ~/einvestor.ru/laravel

# 3. Получение последних изменений
git pull origin main

# 4. Очистка кеша (ВАЖНО для применения изменений)
php8.3 artisan optimize:clear

# 5. Кеширование конфигурации
php8.3 artisan config:cache

# 6. Кеширование маршрутов
php8.3 artisan route:cache

# 7. Кеширование представлений
php8.3 artisan view:cache
```

### Вариант 2: Быстрая команда (одной строкой)

```bash
ssh adminfeg@narnia "cd ~/einvestor.ru/laravel && git pull origin main && php8.3 artisan optimize:clear && php8.3 artisan config:cache && php8.3 artisan route:cache && php8.3 artisan view:cache"
```

## После деплоя проверьте:

1. **Проверьте редирект в админке:**
   - Убедитесь, что редирект создан:
     - Старый URL: `articles/zvukovye-signaly-rsi-teper-i-na-mt5` (без начального слеша)
     - Новый URL: `/products/rsi-alerts-mt5` (с начальным слешем)
     - Активен: ✅ (галочка установлена)

2. **Проверьте редирект в браузере:**
   ```bash
   # Откройте в режиме инкогнито (чтобы исключить кеш браузера)
   https://einvestor.ru/articles/zvukovye-signaly-rsi-teper-i-na-mt5
   ```
   Должен произойти редирект на `/products/rsi-alerts-mt5`

3. **Проверьте через команду (если есть доступ к серверу):**
   ```bash
   php8.3 artisan redirects:test articles/zvukovye-signaly-rsi-teper-i-na-mt5
   ```

## Если редирект все еще не работает:

1. **Проверьте логи:**
   ```bash
   tail -f storage/logs/laravel.log
   ```

2. **Проверьте, что редирект активен в базе данных:**
   ```bash
   php8.3 artisan tinker
   ```
   Затем в tinker:
   ```php
   \App\Models\Redirect::where('old_url', 'articles/zvukovye-signaly-rsi-teper-i-na-mt5')->first();
   ```

3. **Очистите кеш еще раз:**
   ```bash
   php8.3 artisan cache:clear
   php8.3 artisan config:clear
   php8.3 artisan route:clear
   php8.3 artisan view:clear
   ```

## Что было исправлено:

- **Проблема:** Редиректы не работали для путей, начинающихся с `articles`, `products`, `category`, `tag` и т.д., потому что эти пути исключались ДО проверки редиректов.

- **Решение:** Изменен порядок проверок в `RedirectMiddleware`:
  1. Сначала проверяются редиректы для ВСЕХ путей
  2. Если редирект найден - выполняется редирект
  3. Если редирект не найден и путь начинается с исключенного префикса - пропускается дальше в роутинг

## Файлы, которые были изменены:

- `app/Http/Middleware/RedirectMiddleware.php` - исправлена логика проверки редиректов
- `app/Console/Commands/TestRedirectsCommand.php` - добавлена команда для тестирования редиректов
- `REDIRECT_TROUBLESHOOTING.md` - добавлена документация по диагностике редиректов
