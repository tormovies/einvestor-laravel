# EInvestor Laravel Project

Новый проект на Laravel для сайта einvestor.ru

## ✅ Что готово

- ✅ Структура базы данных (16 таблиц)
- ✅ Модели с отношениями (11 моделей)
- ✅ Команда импорта данных из WordPress
- ✅ Роутинг с новой структурой URL
- ✅ Middleware для 301 редиректов
- ✅ Базовые контроллеры

## ⚠️ ВАЖНО: Запуск сервера

**`php artisan serve` НЕ РАБОТАЕТ на Windows!**

Используйте встроенный PHP сервер напрямую:

```bash
cd c:\projects\einvestor-laravel\public
php -S localhost:8000
```

Или дважды кликните на: **`START_SERVER_DIRECT.bat`**

**Подробная инструкция:** `SERVER_START_INSTRUCTIONS.md`

---

## 🚀 Быстрый старт

### 1. Настройка .env

Скопируйте `.env.example` в `.env` и настройте:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=einvestor_laravel
DB_USERNAME=root
DB_PASSWORD=fRAxngtck8um
```

### 2. Создание базы данных

```sql
CREATE DATABASE einvestor_laravel CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. Запуск миграций

```bash
php artisan migrate
```

### 4. Импорт данных из WordPress

```bash
php artisan import:wordpress --path=../einvestor.ru/wordpress-export
```

### 5. Запуск сервера

**⚠️ ВАЖНО:** `php artisan serve` не работает на Windows!

```bash
cd public
php -S localhost:8000
```

Или дважды кликните на: **`START_SERVER_DIRECT.bat`**

Сайт будет доступен по адресу: http://localhost:8000

**📖 Подробная инструкция:** `SERVER_START_INSTRUCTIONS.md`

## 📁 Структура URL

- **Главная:** `/`
- **Статьи:** `/articles` и `/articles/{slug}`
- **Страницы:** `/{slug}`
- **Товары:** `/products` и `/products/{slug}`
- **Категории:** `/category/{slug}`
- **Теги:** `/tag/{slug}`

## 🔄 Редиректы

Все старые URL из WordPress автоматически редиректятся на новые через middleware `RedirectMiddleware`.

## 📝 Следующие шаги

1. Создать views (Blade шаблоны)
2. Интегрировать Робокассу для оплаты
3. Создать корзину и процесс заказа
4. Защита файлов (скачивание только для купивших)
5. Личный кабинет
6. Админ-панель

## 📊 Статистика импорта

- 38 постов
- 17 страниц
- 14 товаров
- 23 категории
- 58 тегов
- 5 категорий товаров
- 158 медиафайлов
