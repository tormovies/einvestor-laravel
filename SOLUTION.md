# 🔍 Решение проблемы с запуском сервера

## Проблема

Команда `php artisan serve` не работает с ошибкой:
```
Failed to listen on 127.0.0.1:8000 (reason: ?)
```

## Причина

Проблема в Laravel команде `serve`, которая использует Symfony Process для запуска встроенного PHP сервера. На Windows это может вызывать проблемы с правами доступа или запуском процесса.

## ✅ Решение: Используйте встроенный PHP сервер напрямую

Вместо `php artisan serve` используйте:

### Способ 1: Через public папку (РЕКОМЕНДУЕТСЯ)

```bash
cd c:\projects\einvestor-laravel\public
php -S localhost:8000
```

### Способ 2: Через batch файл

Дважды кликните на: **`START_SERVER_DIRECT.bat`**

### Способ 3: Через PowerShell

```powershell
cd c:\projects\einvestor-laravel\public
php -S localhost:8000
```

## ✅ Проверено

- ✅ Встроенный PHP сервер (`php -S`) работает нормально
- ✅ Порт 8000 доступен и свободен
- ✅ PHP работает корректно (версия 8.4.15)
- ✅ Laravel проект настроен правильно

## 🌐 URL

После запуска откройте:
**http://localhost:8000**

## ⚠️ Важно

1. **Запускайте сервер из папки `public`**, а не из корня проекта
2. **Не закрывайте окно терминала** пока сервер работает
3. **Остановка:** нажмите `Ctrl+C` в терминале

## 📋 Разница между командами

- `php artisan serve` - использует Laravel команду (проблема на Windows)
- `php -S localhost:8000` - встроенный PHP сервер (работает нормально)

Оба варианта делают одно и то же - запускают встроенный PHP сервер, но `php -S` работает напрямую без обертки Laravel.

---

**Результат:** Сервер успешно запущен на порту 8000 через `php -S localhost:8000`
