# 📋 Пошаговая инструкция: Настройка нового сервера (понятная версия)

**Важно:** Сначала нужно понять структуру вашего нового сервера!

---

## 🔍 ВАЖНО: Где находится ваш сайт на новом сервере?

На разных хостингах структура может отличаться. Нужно узнать:

### Вариант A: Если это Linux сервер (VPS/выделенный сервер)

Обычно сайты находятся в:
- `/var/www/` - стандартная папка для сайтов
- `/var/www/html/` - если сайт один
- `/home/username/public_html/` - на некоторых хостингах
- `/usr/share/nginx/html/` - еще один вариант

**Как узнать где ваш сайт:**

```bash
# На новом сервере - посмотреть конфигурацию Nginx/Apache
cat /etc/nginx/sites-enabled/* | grep root
# или
cat /etc/apache2/sites-enabled/* | grep DocumentRoot
```

Это покажет где находится "корень" сайта (DocumentRoot или root).

### Вариант B: Если это панель управления (cPanel, Plesk и т.д.)

Обычно сайты находятся в:
- `public_html/` - корень сайта для веб-сервера
- `domains/einvestor.ru/public_html/` - если несколько доменов

---

## 📂 Структура Laravel проекта

**Важно понять:**
- Laravel устанавливается в **отдельную папку** (например: `einvestor.ru/`)
- Но веб-сервер должен смотреть в **подпапку `public/`** внутри Laravel

```
einvestor.ru/              <- Весь Laravel проект (можно назвать как угодно)
├── app/                   <- Код приложения
├── public/                <- ЭТОТ путь должен быть "корнем сайта" в Nginx/Apache!
│   ├── index.php         <- Точка входа для веб-сервера
│   └── ...
├── storage/               <- Файлы (товары, логи)
├── .env                   <- Конфигурация
└── ...
```

**В настройке Nginx/Apache:** корень сайта должен быть `/путь/к/einvestor.ru/public`

---

## ✅ Шаг 1: Определить где устанавливать Laravel

**На новом сервере - узнать где находится корень сайта:**

```bash
# Вариант 1: Посмотреть конфигурацию Nginx
cat /etc/nginx/sites-enabled/default | grep root
# Или если уже есть конфиг для einvestor.ru:
cat /etc/nginx/sites-enabled/* | grep root

# Вариант 2: Посмотреть конфигурацию Apache
cat /etc/apache2/sites-enabled/*.conf | grep DocumentRoot

# Вариант 3: Если используете панель управления (cPanel/Plesk)
# - Зайти в панель управления
# - Найти настройки домена einvestor.ru
# - Посмотреть "Корневая директория" или "Document Root"
```

**Результат покажет что-то вроде:**
- `/var/www/html` - значит клонировать в `/var/www/einvestor.ru`
- `/home/user/public_html` - значит клонировать в `/home/user/einvestor.ru`
- `public_html/` - значит клонировать в `einvestor.ru/` рядом с `public_html/`

**Пример:** Если веб-сервер смотрит в `/var/www/html`, то:
- Laravel клонировать в: `/var/www/einvestor.ru/`
- А в конфиге Nginx указать: `root /var/www/einvestor.ru/public;`

---

## ✅ Шаг 2: Клонирование репозитория

**Замените `/var/www/einvestor.ru` на вашу реальную папку!**

**Пример 1: Если веб-сервер смотрит в `/var/www/html`:**

```bash
# Создать папку для Laravel
cd /var/www
mkdir einvestor.ru

# Или сразу клонировать (создаст папку автоматически)
cd /var/www
git clone https://github.com/tormovies/einvestor-laravel.git einvestor.ru
```

**Пример 2: Если веб-сервер смотрит в `/home/user/public_html`:**

```bash
cd /home/user
git clone https://github.com/tormovies/einvestor-laravel.git einvestor.ru
```

**Пример 3: Если используете панель управления (cPanel/Plesk):**

Обычно структура такая:
```
/home/user/
├── public_html/          <- Текущий корень сайта
└── einvestor.ru/         <- Сюда клонировать Laravel
    └── public/           <- А в конфиге указать этот путь как корень
```

```bash
# Перейти в домашнюю директорию
cd /home/user  # или cd ~

# Клонировать Laravel
git clone https://github.com/tormovies/einvestor-laravel.git einvestor.ru
```

**✅ Проверка:** Должна появиться папка с проектом Laravel. Проверить:
```bash
cd einvestor.ru
ls -la
# Должны увидеть: app/, public/, storage/, .env.example и т.д.
```

---

## ✅ Шаг 3: Установка зависимостей

```bash
# Перейти в папку проекта
cd /путь/к/einvestor.ru  # Заменить на реальный путь!

# Установить зависимости
composer install --no-dev --optimize-autoloader
```

**⚠️ Если ошибка "composer: command not found":**
```bash
# Установить Composer (если нет)
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
```

---

## ✅ Шаг 4: Настройка .env

```bash
cd /путь/к/einvestor.ru  # Заменить на реальный путь!

# Скопировать шаблон
cp .env.example .env

# Сгенерировать ключ
php artisan key:generate

# Отредактировать .env
nano .env  # или другой редактор
```

**В `.env` изменить:**
- `APP_ENV=production`
- `APP_DEBUG=false`
- `APP_URL=https://einvestor.ru` (или http:// если SSL еще нет)
- Настройки БД (будем создавать на следующем шаге)
- Настройки Робокассы (продакшн данные)

---

## ✅ Шаг 5: Создание базы данных

**Создать БД можно двумя способами:**

### Способ 1: Через MySQL консоль

```bash
mysql -u root -p
# Ввести пароль root

CREATE DATABASE einvestor_prod CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'einvestor_user'@'localhost' IDENTIFIED BY 'ВАШ_ПАРОЛЬ';
GRANT ALL PRIVILEGES ON einvestor_prod.* TO 'einvestor_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### Способ 2: Через панель управления хостингом (cPanel/Plesk)

1. Зайти в панель управления хостингом
2. Найти "Базы данных MySQL" или "MySQL Databases"
3. Создать новую базу данных: `einvestor_prod`
4. Создать пользователя: `einvestor_user` с паролем
5. Привязать пользователя к базе данных (дать права)

**Записать:**
- Имя БД: `einvestor_prod` (или как создали)
- Пользователь: `einvestor_user` (или как создали)
- Пароль: тот что указали

**✅ Проверка:** Записать эти данные - они понадобятся в `.env` файле!

---

## ✅ Шаг 6: Обновить .env с настройками БД

```bash
nano /путь/к/einvestor.ru/.env
```

**Изменить раздел базы данных:**

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=einvestor_prod          # Имя БД из шага 5
DB_USERNAME=einvestor_user          # Пользователь из шага 5
DB_PASSWORD=ВАШ_ПАРОЛЬ_БД           # Пароль из шага 5
```

**Сохранить:** `Ctrl+O` → Enter → `Ctrl+X` (в nano)

---

## ✅ Шаг 7: Применение миграций

```bash
cd /путь/к/einvestor.ru

# Проверить подключение к БД
php artisan migrate:status

# Применить миграции (создать таблицы)
php artisan migrate --force
```

**✅ Проверка:** Все миграции применены без ошибок.

---

## ✅ Шаг 8: Импорт данных из WordPress

**Если есть JSON файл:**

```bash
# Загрузить файл на сервер (с вашего компьютера через FileZilla/SCP)
# Поместить в: /путь/к/einvestor.ru/

# На сервере:
cd /путь/к/einvestor.ru
php artisan import:wordpress wordpress-export.json
```

---

## ✅ Шаг 9: Копирование файлов товаров

**Создать папку для товаров:**

```bash
cd /путь/к/einvestor.ru
mkdir -p storage/app/products
chmod 775 storage/app/products
```

**Скопировать файлы со старого сервера:**
- Через FTP (FileZilla) - загрузить файлы в `storage/app/products/`
- Или через SCP (командная строка)

**✅ Проверка:** Файлы товаров есть в `storage/app/products/`.

---

## ✅ Шаг 10: Настройка веб-сервера (ВАЖНО!)

**Нужно изменить конфигурацию так, чтобы корень сайта указывал на `public/` папку Laravel!**

### Если Nginx:

```bash
# Найти конфигурацию для вашего домена
sudo nano /etc/nginx/sites-available/einvestor.ru
# или если нет - создать новый
```

**В конфиге изменить/установить:**

```nginx
server {
    listen 80;
    server_name einvestor.ru www.einvestor.ru;
    
    # ВАЖНО: Указать путь к public/ внутри Laravel проекта!
    root /путь/к/einvestor.ru/public;  # Заменить на реальный путь!
    
    index index.php;
    
    # ... остальная конфигурация из NEW_SERVER_STEPS.md
}
```

### Если Apache:

```bash
sudo nano /etc/apache2/sites-available/einvestor.ru.conf
```

**В конфиге изменить:**

```apache
<VirtualHost *:80>
    ServerName einvestor.ru
    
    # ВАЖНО: Указать путь к public/ внутри Laravel проекта!
    DocumentRoot /путь/к/einvestor.ru/public  # Заменить на реальный путь!
    
    # ... остальная конфигурация
</VirtualHost>
```

### Если панель управления (cPanel/Plesk):

1. Зайти в настройки домена `einvestor.ru`
2. Найти "Document Root" или "Корневая директория"
3. Изменить на: `/путь/к/einvestor.ru/public`
4. Сохранить

**⚠️ Важно:** Всегда указывать путь к `public/`, а не к корню Laravel проекта!

**✅ Проверка:** После перезагрузки Nginx/Apache сайт должен открываться.

---

## ✅ Шаг 11: Установка прав доступа

```bash
cd /путь/к/einvestor.ru

# Если это Linux сервер - установить права
sudo chown -R www-data:www-data .
sudo chmod -R 775 storage bootstrap/cache

# Если панель управления - может понадобиться другая команда
# (проверить в документации хостинга)
```

---

## ✅ Шаг 12: Оптимизация и тестирование

```bash
cd /путь/к/einvestor.ru

php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

**Проверить сайт** - должен открываться в браузере!

---

## 📝 Краткая памятка по путям

**Пример для типичного Linux сервера:**

```
/var/www/                    <- Папка для сайтов
└── einvestor.ru/            <- Laravel проект (клонировать сюда)
    ├── app/                 
    ├── public/              <- КОРЕНЬ САЙТА (указывать в Nginx/Apache)
    │   └── index.php       
    ├── storage/             <- Файлы товаров идут сюда
    └── .env                 
```

**В конфиге Nginx/Apache указывать:**
```
root /var/www/einvestor.ru/public;
```

---

## ❓ Как узнать правильный путь на вашем сервере?

**Если не знаете структуру вашего сервера:**

1. **Спросите у хостинг-провайдера:**
   - "Где находится корневая директория для домена?"
   - "Как настроить Document Root для домена?"

2. **Или посмотрите в панели управления:**
   - cPanel → File Manager → посмотреть структуру
   - Plesk → Domains → Document Root

3. **Или проверьте текущую конфигурацию:**
   ```bash
   # На сервере
   cat /etc/nginx/sites-enabled/* | grep root
   ```

---

**После того как определите путь - используйте его везде где написано `/путь/к/einvestor.ru`!**

Нужна помощь определить структуру вашего сервера? Можете сказать какая у вас панель управления или хостинг?
