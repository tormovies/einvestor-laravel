# 📋 Пошаговая инструкция: Настройка нового сервера

**Для:** Новый сервер для Laravel (старый WordPress остается работать)

---

## ✅ Шаг 1: Проверка требований на новом сервере

**Выполнить на новом сервере:**

```bash
# Проверить PHP версию (нужна 8.2+)
php -v

# Проверить Composer (должен быть установлен)
composer --version

# Проверить MySQL (должен быть установлен)
mysql --version

# Проверить Git (должен быть установлен)
git --version

# Проверить Nginx или Apache
nginx -v  # или
apache2 -v
```

**Если чего-то нет - установить:**

```bash
# PHP 8.2+ (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install php8.2 php8.2-fpm php8.2-mysql php8.2-mbstring php8.2-xml php8.2-curl php8.2-zip

# Composer
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer

# MySQL
sudo apt-get install mysql-server

# Nginx
sudo apt-get install nginx

# Git
sudo apt-get install git
```

**✅ Проверка:** Все команды должны показывать версии, без ошибок.

---

## ✅ Шаг 2: Клонирование репозитория

**Выполнить на новом сервере:**

```bash
# Перейти в директорию для сайтов
cd /var/www

# Клонировать репозиторий с GitHub
git clone https://github.com/tormovies/einvestor-laravel.git einvestor.ru

# Перейти в директорию проекта
cd einvestor.ru

# Проверить, что все файлы скопированы
ls -la
```

**✅ Проверка:** Должна появиться папка `/var/www/einvestor.ru` с файлами Laravel.

---

## ✅ Шаг 3: Установка зависимостей Composer

**Выполнить на новом сервере:**

```bash
cd /var/www/einvestor.ru

# Установить зависимости (это может занять несколько минут)
composer install --no-dev --optimize-autoloader
```

**⚠️ Если возникнут ошибки с правами:**
```bash
# Установить права для Composer
sudo chown -R $USER:$USER /var/www/einvestor.ru
```

**✅ Проверка:** После установки должна появиться папка `vendor/` с зависимостями.

---

## ✅ Шаг 4: Настройка окружения (.env)

**Выполнить на новом сервере:**

```bash
cd /var/www/einvestor.ru

# Скопировать шаблон .env
cp .env.example .env

# Сгенерировать APP_KEY
php artisan key:generate
```

**Теперь отредактировать `.env` файл:**

```bash
# Открыть в редакторе (nano, vim или другой)
nano .env
```

**Обязательно изменить в `.env`:**

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://einvestor.ru

# База данных (будем создавать на следующем шаге)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=einvestor_prod
DB_USERNAME=einvestor_user
DB_PASSWORD=ВАШ_ПАРОЛЬ_БД

# Робокасса (продакшн данные!)
ROBOKASSA_MERCHANT_LOGIN=ваш_логин
ROBOKASSA_PASSWORD1=ваш_пароль1
ROBOKASSA_PASSWORD2=ваш_пароль2
ROBOKASSA_HASH_TYPE=md5
ROBOKASSA_IS_TEST=false
```

**Сохранить файл:**
- В nano: `Ctrl+O` → Enter → `Ctrl+X`
- В vim: `Esc` → `:wq` → Enter

**✅ Проверка:** `.env` файл существует и содержит правильные данные.

---

## ✅ Шаг 5: Создание базы данных

**Выполнить на новом сервере:**

```bash
# Войти в MySQL
sudo mysql -u root -p
# (потребуется пароль root для MySQL)

# В MySQL консоли выполнить:
CREATE DATABASE einvestor_prod CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE USER 'einvestor_user'@'localhost' IDENTIFIED BY 'ВАШ_ПАРОЛЬ_БД';

GRANT ALL PRIVILEGES ON einvestor_prod.* TO 'einvestor_user'@'localhost';

FLUSH PRIVILEGES;

# Проверить, что база создана
SHOW DATABASES;

# Выйти из MySQL
EXIT;
```

**⚠️ Важно:** Заменить `ВАШ_ПАРОЛЬ_БД` на реальный надежный пароль и использовать тот же пароль в `.env` файле!

**✅ Проверка:** База данных `einvestor_prod` существует, пользователь `einvestor_user` создан.

---

## ✅ Шаг 6: Применение миграций

**Выполнить на новом сервере:**

```bash
cd /var/www/einvestor.ru

# Проверить подключение к БД
php artisan migrate:status

# Применить миграции (создать таблицы)
php artisan migrate --force
```

**✅ Проверка:** Все миграции применены без ошибок, таблицы созданы в БД.

---

## ✅ Шаг 7: Импорт данных из WordPress

**Вариант A: Если есть JSON файл с экспортом WordPress**

```bash
# Загрузить JSON файл на сервер (с вашего компьютера)
scp wordpress-export.json user@new-server:/tmp/

# На сервере - импортировать
cd /var/www/einvestor.ru
php artisan import:wordpress /tmp/wordpress-export.json
```

**Вариант B: Экспорт из старой БД (со старого сервера)**

```bash
# На СТАРОМ сервере - экспорт БД
mysqldump -u root -p adminfeg_einvest > einvestor-export.sql

# Загрузить SQL файл на НОВЫЙ сервер
scp einvestor-export.sql user@new-server:/tmp/

# На НОВОМ сервере - создать БД и импортировать данные
# (требуется скрипт для парсинга SQL и импорта в Laravel)
# Или использовать команду импорта, если она поддерживает SQL
```

**✅ Проверка:** Данные импортированы - проверить в БД:
```bash
mysql -u einvestor_user -p einvestor_prod
SELECT COUNT(*) FROM products;
SELECT COUNT(*) FROM posts;
EXIT;
```

---

## ✅ Шаг 8: Копирование файлов товаров

**Выполнить:**

```bash
# На НОВОМ сервере - создать папку для товаров
cd /var/www/einvestor.ru
mkdir -p storage/app/products
chmod -R 775 storage/app/products

# Скопировать файлы со СТАРОГО сервера (выполнить на вашем компьютере или старом сервере)
scp -r user@old-server:/var/www/einvestor.ru/wp-content/uploads/woocommerce_uploads/* \
      user@new-server:/var/www/einvestor.ru/storage/app/products/
```

**Или через rsync (если установлен):**

```bash
rsync -avz --progress user@old-server:/var/www/einvestor.ru/wp-content/uploads/woocommerce_uploads/ \
                     user@new-server:/var/www/einvestor.ru/storage/app/products/
```

**✅ Проверка:** Файлы скопированы в `storage/app/products/`:
```bash
ls -la /var/www/einvestor.ru/storage/app/products/
```

---

## ✅ Шаг 9: Настройка Nginx

**Выполнить на новом сервере:**

```bash
# Создать конфигурацию для сайта
sudo nano /etc/nginx/sites-available/einvestor.ru
```

**Вставить следующий конфиг (сначала БЕЗ SSL):**

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name einvestor.ru www.einvestor.ru;
    
    root /var/www/einvestor.ru/public;
    index index.php;
    
    charset utf-8;
    
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }
    
    error_page 404 /index.php;
    
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }
    
    location ~ /\.(?!well-known).* {
        deny all;
    }
    
    # Защита storage
    location ~ ^/storage/ {
        deny all;
    }
    
    client_max_body_size 20M;
}
```

**Сохранить и активировать:**

```bash
# Создать символическую ссылку
sudo ln -s /etc/nginx/sites-available/einvestor.ru /etc/nginx/sites-enabled/

# Проверить конфигурацию
sudo nginx -t

# Если все ОК - перезагрузить Nginx
sudo systemctl reload nginx
```

**✅ Проверка:** Nginx конфигурация без ошибок, сайт доступен по IP (пока без домена).

---

## ✅ Шаг 10: Установка прав доступа

**Выполнить на новом сервере:**

```bash
cd /var/www/einvestor.ru

# Установить владельца (www-data - стандартный пользователь веб-сервера)
sudo chown -R www-data:www-data .

# Установить права для файлов и папок
sudo find . -type f -exec chmod 644 {} \;
sudo find . -type d -exec chmod 755 {} \;

# Особые права для storage и cache (Laravel должен писать сюда)
sudo chmod -R 775 storage bootstrap/cache
sudo chown -R www-data:www-data storage bootstrap/cache

# Защитить .env файл
sudo chmod 600 .env
sudo chown www-data:www-data .env
```

**✅ Проверка:** Права установлены, Laravel может писать в `storage/`.

---

## ✅ Шаг 11: Оптимизация Laravel для продакшена

**Выполнить на новом сервере:**

```bash
cd /var/www/einvestor.ru

# Очистить весь кеш
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear

# Создать кеш для продакшена (ускорит работу)
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize

# Оптимизировать автозагрузку Composer
composer dump-autoload --optimize --no-dev
```

**✅ Проверка:** Кеш создан, сайт работает быстрее.

---

## ✅ Шаг 12: Установка SSL сертификата (Let's Encrypt)

**Выполнить на новом сервере (после того как DNS настроен на новый сервер):**

```bash
# Установить Certbot
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx

# Получить SSL сертификат (домен должен указывать на этот сервер!)
sudo certbot --nginx -d einvestor.ru -d www.einvestor.ru

# Проверить автоматическое обновление
sudo certbot renew --dry-run
```

**⚠️ Важно:** DNS должен указывать на новый сервер перед получением SSL!

**После установки SSL** - Certbot автоматически обновит конфигурацию Nginx для HTTPS.

**✅ Проверка:** HTTPS работает, сайт открывается по `https://einvestor.ru`.

---

## ✅ Шаг 13: Тестирование нового сайта

**Проверить по IP или временному домену:**

```bash
# Посмотреть логи при ошибках
tail -f /var/www/einvestor.ru/storage/logs/laravel.log
```

**Чеклист проверки:**

- [ ] Главная страница открывается (по IP или временному домену)
- [ ] Товары отображаются (`/products`)
- [ ] Блог работает (`/articles`)
- [ ] Корзина работает (`/cart`)
- [ ] Оформление заказа работает (`/checkout`)
- [ ] Админ-панель доступна (`/admin` - вход с `admin@einvestor.ru` / `password`)
- [ ] Личный кабинет работает (`/account` - после входа)

---

## ✅ Шаг 14: Переключение DNS (финальный шаг)

**Когда новый сайт полностью протестирован и работает:**

1. **Войти в панель управления DNS** (где настраивали домен)

2. **Изменить A-запись:**
   ```
   Старая: einvestor.ru → OLD_SERVER_IP
   Новая:  einvestor.ru → NEW_SERVER_IP
   ```

3. **Подождать распространения DNS (5-30 минут, максимум 48 часов)**

4. **Проверить переключение:**
   ```bash
   nslookup einvestor.ru
   # Должен показать новый IP сервера
   ```

5. **Старый сервер не трогать!** - оставить как бэкап

---

## 🔄 Обновления в будущем

**Когда нужно обновить код на новом сервере:**

```bash
cd /var/www/einvestor.ru

# Обновить код из GitHub
git pull origin main

# Обновить зависимости (если composer.json изменился)
composer install --no-dev --optimize-autoloader

# Применить новые миграции (если есть)
php artisan migrate --force

# Очистить и пересоздать кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

---

## ⚠️ Важные напоминания

1. **Старый сервер НЕ трогать** - пусть работает как бэкап
2. **Тестировать на новом сервере** перед переключением DNS
3. **Мониторить логи** после переключения: `tail -f storage/logs/laravel.log`
4. **Иметь план отката** - если что-то пойдет не так, вернуть DNS на старый сервер

---

## 🆘 Если что-то пошло не так

**Быстрый откат:**

1. Вернуть DNS на старый сервер в панели управления DNS
2. Старый сайт продолжит работать
3. Новый сервер оставить - можно исправить проблемы
4. После исправлений - повторить переключение DNS

---

**Готово! После выполнения всех шагов новый Laravel сайт будет работать на новом сервере! 🚀**
