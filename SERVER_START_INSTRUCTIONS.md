# 🚀 Инструкция по запуску Laravel сервера

## ⚠️ ВАЖНО: Проблема с `php artisan serve` на Windows

**Команда `php artisan serve` НЕ РАБОТАЕТ** на Windows и выдает ошибку:
```
Failed to listen on 127.0.0.1:8000 (reason: ?)
```

### Причина
Laravel команда `serve` использует Symfony Process для запуска встроенного PHP сервера, что вызывает проблемы с правами доступа или запуском процесса на Windows.

## ✅ РЕШЕНИЕ: Используйте встроенный PHP сервер напрямую

### Способ 1: Через папку public (РЕКОМЕНДУЕТСЯ)

```bash
cd c:\projects\einvestor-laravel\public
php -S localhost:8000
```

**После запуска вы увидите:**
```
PHP 8.4.15 Development Server (http://localhost:8000) started
```

**URL:** http://localhost:8000

---

### Способ 2: Через batch файл

Дважды кликните на: **`START_SERVER_DIRECT.bat`**

Этот файл автоматически:
- Переходит в папку `public`
- Запускает `php -S localhost:8000`

---

### Способ 3: Через PowerShell скрипт

```powershell
cd c:\projects\einvestor-laravel\public
php -S localhost:8000
```

---

## 📋 Полная последовательность запуска

### Шаг 1: Откройте терминал
- **PowerShell** или **CMD**

### Шаг 2: Перейдите в папку public
```bash
cd c:\projects\einvestor-laravel\public
```

### Шаг 3: Запустите сервер
```bash
php -S localhost:8000
```

### Шаг 4: Откройте в браузере
```
http://localhost:8000
```

### Шаг 5: Остановка сервера
Нажмите `Ctrl+C` в терминале

---

## ⚠️ ВАЖНЫЕ ЗАМЕТКИ

### 1. Запуск из папки `public`
**ВАЖНО:** Запускайте сервер из папки `public`, а не из корня проекта!

```bash
# ❌ НЕПРАВИЛЬНО
cd c:\projects\einvestor-laravel
php -S localhost:8000

# ✅ ПРАВИЛЬНО
cd c:\projects\einvestor-laravel\public
php -S localhost:8000
```

### 2. Не закрывайте окно терминала
Сервер работает только пока открыто окно терминала. При закрытии окна сервер останавливается.

### 3. Порт 8000
Если порт 8000 занят, используйте другой:
```bash
php -S localhost:8001
```

---

## 🔍 Проверка работы сервера

### В терминале должно быть:
```
PHP 8.4.15 Development Server (http://localhost:8000) started
```

### Проверка порта:
```powershell
netstat -ano | findstr ":8000" | findstr "LISTENING"
```

### Проверка в браузере:
Откройте http://localhost:8000 - должна отображаться главная страница Laravel

---

## 🆚 Разница между командами

| Команда | Работает? | Описание |
|---------|-----------|----------|
| `php artisan serve` | ❌ НЕТ (Windows) | Laravel команда - проблема с Symfony Process |
| `php -S localhost:8000` | ✅ ДА | Встроенный PHP сервер - работает нормально |

**Обе команды делают одно и то же** - запускают встроенный PHP сервер, но `php -S` работает напрямую без обертки Laravel.

---

## 🔧 Альтернативные порты

Если порт 8000 занят:

```bash
# Порт 8001
php -S localhost:8001

# Порт 3000
php -S localhost:3000

# Порт 8080
php -S localhost:8080
```

---

## 📝 Быстрая справка

### Запуск (одна команда)
```bash
cd c:\projects\einvestor-laravel\public && php -S localhost:8000
```

### Остановка
```
Ctrl+C в терминале
```

### URL проекта
```
http://localhost:8000
```

---

## ⚡ Самый быстрый способ

**Дважды кликните на:** `START_SERVER_DIRECT.bat`

Всё остальное произойдет автоматически.

---

## ❓ Частые вопросы

### Q: Почему не работает `php artisan serve`?
**A:** Проблема с Symfony Process на Windows. Используйте `php -S` напрямую.

### Q: Какой порт использовать?
**A:** По умолчанию 8000. Если занят - используйте 8001, 3000 или 8080.

### Q: Нужно ли перезапускать сервер после изменений в коде?
**A:** Нет, встроенный PHP сервер автоматически перезагружает PHP код при изменении файлов.

### Q: Что делать если сервер не запускается?
**A:** 
1. Проверьте, что PHP установлен: `php -v`
2. Проверьте, что вы в папке `public`: `cd c:\projects\einvestor-laravel\public`
3. Проверьте, что порт свободен: `netstat -ano | findstr ":8000"`

---

**Последнее обновление:** 18 января 2026  
**Проверено на:** Windows 10/11, PHP 8.4.15, Laravel 12.47.0
