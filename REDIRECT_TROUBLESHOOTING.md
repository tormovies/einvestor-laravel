# 🔧 Диагностика проблем с редиректами

## ✅ Что уже сделано:

1. ✅ Очищен кеш Laravel (`cache:clear`, `config:clear`, `route:clear`, `view:clear`)
2. ✅ Проверено, что все редиректы активны (154 активных, 0 неактивных)
3. ✅ Проверено, что `RedirectMiddleware` зарегистрирован в `bootstrap/app.php`
4. ✅ Создана команда для проверки редиректов: `php artisan redirects:test`

## 🔍 Как проверить редирект:

### 1. Проверка через команду:
```bash
php artisan redirects:test ваш-url-без-слеша
```

Например:
```bash
php artisan redirects:test caldera_forms_preview
```

### 2. Проверка в браузере:
1. Откройте браузер в режиме инкогнито (чтобы исключить кеш браузера)
2. Перейдите по старому URL: `http://localhost:8000/ваш-старый-url`
3. Должен произойти редирект на новый URL

### 3. Проверка через DevTools:
1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Перейдите по старому URL
4. Проверьте, что первый запрос возвращает код 301 или 302
5. Проверьте заголовок `Location` в ответе

## ⚠️ Возможные проблемы:

### 1. Редирект неактивен
**Решение:** В админке проверьте, что у редиректа стоит галочка "Активен"

### 2. Неправильный формат URL
**Важно:** 
- `old_url` должен быть **БЕЗ** начального слеша (например: `staryj-url`)
- `new_url` должен быть **С** начальным слешем (например: `/novyj-url`)

### 3. URL попадает в исключения
Редиректы **НЕ работают** для путей, начинающихся с:
- `articles`
- `products`
- `category`
- `tag`
- `admin`
- `cart`
- `checkout`
- `download`
- `login`
- `logout`
- `account`
- `api`
- `robokassa`

Если ваш старый URL начинается с одного из этих префиксов, редирект не сработает.

### 4. Кеш браузера
**Решение:** Используйте режим инкогнито или очистите кеш браузера (Ctrl+Shift+Delete)

### 5. Редирект на самого себя
Если `old_url` и `new_url` одинаковые, редирект не сработает (защита от циклов)

## 🛠️ Команды для диагностики:

```bash
# Очистить весь кеш
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# Проверить редирект
php artisan redirects:test ваш-url

# Посмотреть статистику редиректов
php artisan redirects:test
```

## 📝 Если редирект все еще не работает:

1. Проверьте, что редирект активен в админке
2. Проверьте формат URL (без слеша для old_url, со слешем для new_url)
3. Проверьте, что URL не начинается с исключенных префиксов
4. Попробуйте в режиме инкогнито
5. Проверьте логи Laravel: `storage/logs/laravel.log`
6. Используйте команду `php artisan redirects:test ваш-url` для проверки

## 🔄 После добавления нового редиректа:

После добавления нового редиректа в админке:
1. Кеш уже очищен автоматически
2. Редирект должен работать сразу
3. Если не работает - проверьте по инструкции выше
