# Команды для деплоя на продакшен

**Путь к проекту на продакшене:** `adminfeg@narnia:~/einvestor.ru/laravel`  
**Версия PHP:** `php8.3` (использовать для всех команд php и composer)

## 1. Подключение к серверу

```bash
ssh adminfeg@narnia
cd ~/einvestor.ru/laravel
```

## 2. Получение последних изменений из репозитория

```bash
# Переключиться на нужную ветку (если нужно)
git checkout main

# Получить последние изменения
git pull origin main
```

## 3. Установка зависимостей (если нужно)

```bash
php8.3 /usr/bin/composer install --no-dev --optimize-autoloader
# или если composer установлен глобально через php8.3:
php8.3 $(which composer) install --no-dev --optimize-autoloader
npm install --production  # если используете npm
npm run production  # если используете сборку фронтенда
```

## 4. Применение миграций базы данных

```bash
php8.3 artisan migrate --force
```

## 5. Перенос данных из локальной БД (если нужно)

### Вариант A: Экспорт/импорт через mysqldump

**На локалке:**
```bash
mysqldump -u username -p database_name > backup.sql
```

**На продакшене:**
```bash
mysql -u username -p database_name < backup.sql
```

### Вариант B: Использование Laravel Backup (если установлен)

**На локалке:**
```bash
php artisan backup:run
```

**На продакшене:**
```bash
php artisan backup:restore
```

## 6. Перенос файлов изображений

### Вариант A: Через SCP (если SSH работает)

```bash
# С локальной машины
scp -r storage/app/public/products/images adminfeg@narnia:~/einvestor.ru/laravel/storage/app/public/
```

**Если получаете ошибку "Permission denied":**
- Проверьте, что SSH ключи настроены правильно
- Попробуйте указать порт: `scp -P 22 -r ...`
- Или используйте другие варианты ниже

### Вариант B: Через rsync (если SSH работает)

```bash
# С локальной машины
rsync -avz --progress storage/app/public/products/images/ adminfeg@narnia:~/einvestor.ru/laravel/storage/app/public/products/images/
```

### Вариант C: Через FTP/SFTP клиент (FileZilla, WinSCP и т.д.)

1. Подключитесь к серверу через FTP/SFTP клиент
2. Скопируйте папку `storage/app/public/products/images/` на сервер в `~/einvestor.ru/laravel/storage/app/public/`

### Вариант D: Через архив и загрузку (рекомендуется, если SSH не работает)

**На локалке:**
```bash
# Создать архив с изображениями
cd storage/app/public
tar -czf products_images.tar.gz products/images/
# или
zip -r products_images.zip products/images/
```

**Затем:**
1. Загрузите архив на сервер через FTP/SFTP или веб-интерфейс хостинга
2. На сервере распакуйте:
```bash
cd ~/einvestor.ru/laravel/storage/app/public
tar -xzf products_images.tar.gz
# или
unzip products_images.zip
```

### Вариант E: Через веб-интерфейс хостинга

Если у вас есть доступ к файловому менеджеру хостинга (cPanel, Plesk и т.д.):
1. Зайдите в файловый менеджер
2. Перейдите в `~/einvestor.ru/laravel/storage/app/public/`
3. Загрузите папку `products/images/` через веб-интерфейс

## 7. Создание символической ссылки для storage

```bash
php8.3 artisan storage:link
```

## 8. Проверка прав доступа к папкам

```bash
# Убедитесь, что папки имеют правильные права
chmod -R 755 storage
chmod -R 755 bootstrap/cache
chown -R adminfeg:adminfeg storage bootstrap/cache
```

## 9. Выполнение команд исправления ссылок

```bash
# Исправить ссылки на изображения в описаниях товаров
php8.3 artisan products:fix-images-in-description

# Исправить внешние ссылки на локальные
php8.3 artisan links:fix-external
```

## 10. Очистка кеша

```bash
php8.3 artisan config:clear
php8.3 artisan cache:clear
php8.3 artisan route:clear
php8.3 artisan view:clear
php8.3 artisan optimize:clear
```

## 11. Оптимизация (опционально, но рекомендуется)

```bash
php8.3 artisan config:cache
php8.3 artisan route:cache
php8.3 artisan view:cache
php8.3 artisan optimize
```

## 12. Проверка настроек .env

Убедитесь, что в `.env` на продакшене установлены правильные значения:

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://einvestor.ru

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

## 13. Перезапуск сервера (если нужно)

### Для PHP-FPM:
```bash
sudo service php8.3-fpm restart
```

### Для Nginx:
```bash
sudo service nginx reload
```

### Для Apache:
```bash
sudo service apache2 restart
```

## 14. Проверка работы

1. Откройте сайт в браузере
2. Проверьте страницу товара
3. Проверьте, что изображения загружаются
4. Проверьте, что ссылки работают правильно

## Полный скрипт деплоя (можно сохранить как deploy.sh)

```bash
#!/bin/bash

echo "🚀 Начало деплоя..."

# Переход в директорию проекта
cd ~/einvestor.ru/laravel

# Получение изменений
echo "📥 Получение изменений из репозитория..."
git pull origin main

# Установка зависимостей
echo "📦 Установка зависимостей..."
php8.3 $(which composer) install --no-dev --optimize-autoloader

# Применение миграций
echo "🗄️ Применение миграций..."
php8.3 artisan migrate --force

# Создание символической ссылки
echo "🔗 Создание символической ссылки storage..."
php8.3 artisan storage:link

# Исправление ссылок
echo "🔧 Исправление ссылок на изображения..."
php8.3 artisan products:fix-images-in-description

echo "🔧 Исправление внешних ссылок..."
php8.3 artisan links:fix-external

# Очистка кеша
echo "🧹 Очистка кеша..."
php8.3 artisan optimize:clear

# Оптимизация
echo "⚡ Оптимизация..."
php8.3 artisan config:cache
php8.3 artisan route:cache
php8.3 artisan view:cache

echo "✅ Деплой завершен!"
```

## Важные замечания

1. **Резервное копирование**: Перед деплоем обязательно сделайте резервную копию БД и файлов
2. **Тестирование**: Протестируйте на staging окружении перед продакшеном
3. **Изображения**: Убедитесь, что все изображения скопированы на сервер
4. **Права доступа**: Проверьте права доступа к папкам storage и bootstrap/cache
5. **Логи**: Проверьте логи на наличие ошибок: `tail -f storage/logs/laravel.log`

## Откат изменений (если что-то пошло не так)

```bash
# Откатить к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту
git reset --hard <commit-hash>

# Очистить кеш
php artisan optimize:clear
```
