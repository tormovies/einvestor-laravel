# 🔧 Исправление редиректа на главной странице

## Проблема

Главная страница `/` перекидывала на `/refund_returns` из-за редиректа.

## Причина

В `RedirectMiddleware` для главной страницы `/` путь после `trim($request->path(), '/')` становится пустой строкой `''`. Если в базе данных есть редирект с `old_url = ''` (пустая строка), он срабатывал для главной страницы.

## Решение

Добавлена проверка в `RedirectMiddleware` - главная страница (пустой путь) теперь пропускается и обрабатывается через обычный роутинг Laravel:

```php
// Пропускаем главную страницу - она должна обрабатываться роутингом
if ($path === '') {
    return $next($request);
}
```

## Результат

✅ Главная страница `/` больше не редиректится  
✅ Главная страница корректно обрабатывается `HomeController`  
✅ Все остальные редиректы работают нормально

## Если проблема осталась

Если после исправления проблема осталась, проверьте:

1. **Удалите редирект с пустым URL** из базы данных:
   ```php
   \App\Models\Redirect::where('old_url', '')->delete();
   ```

2. **Или отключите его:**
   ```php
   \App\Models\Redirect::where('old_url', '')->update(['is_active' => false]);
   ```
