# 📋 Пошаговая инструкция: Установка Laravel на Beget

**Хостинг:** Beget  
**Домен:** einvestor.ru

---

## 🔍 Структура Beget

На Beget структура обычно такая:

```
/home/u/user/                      <- Ваша домашняя директория
└── domains/
    └── einvestor.ru/              <- Папка для домена
        └── public_html/           <- Корень сайта (текущий Document Root)
```

**Для Laravel нужно:**
1. Клонировать Laravel в `/home/u/user/domains/einvestor.ru/`
2. Изменить Document Root на `/home/u/user/domains/einvestor.ru/public`
   (через панель Beget или настройки домена)

---

## ✅ Шаг 1: Подключение к серверу Beget

**Вариант 1: Через SSH (рекомендуется)**

```bash
# Подключиться к Beget по SSH
ssh ваш_логин@beget.ru
# или
ssh ваш_логин@einvestor.ru
```

**Вариант 2: Через файловый менеджер в панели Beget**

- Зайти в панель управления Beget: https://cp.beget.com
- Найти "Файловый менеджер" или "File Manager"

---

## ✅ Шаг 2: Определить путь к домену

**Найти путь к домену через SSH:**

```bash
# После подключения по SSH - проверить структуру
cd ~
ls -la

# Обычно структура такая:
# /home/u/user/domains/einvestor.ru/

# Перейти в папку домена
cd domains/einvestor.ru
pwd  # Покажет полный путь
```

**Или через панель Beget:**

1. Зайти в панель: https://cp.beget.com
2. Найти "Домены и поддомены" или "Domains"
3. Найти `einvestor.ru`
4. Посмотреть путь (обычно `/home/u/user/domains/einvestor.ru/`)

**📝 Записать путь!** Например: `/home/u/user/domains/einvestor.ru/`

---

## ✅ Шаг 3: Клонирование репозитория

**Через SSH (рекомендуется):**

```bash
# Перейти в папку домена
cd /home/u/user/domains/einvestor.ru/  # Заменить на ваш путь!

# Убедиться что Git установлен
git --version

# Клонировать Laravel проект
git clone https://github.com/tormovies/einvestor-laravel.git .

# Или если уже есть файлы в этой папке:
# cd /home/u/user/domains/
# git clone https://github.com/tormovies/einvestor-laravel.git einvestor.ru
```

**✅ Проверка:** Должны появиться файлы Laravel:
```bash
ls -la
# Должны увидеть: app/, public/, storage/, .env.example и т.д.
```

---

## ✅ Шаг 4: Установка зависимостей Composer

**Важно:** Beget должен поддерживать Composer. Проверить:

```bash
cd /home/u/user/domains/einvestor.ru/  # Ваш путь

# Проверить Composer
composer --version

# Если нет - установить (или использовать глобальный)
# На Beget обычно Composer доступен глобально
```

**Установить зависимости:**

```bash
# Перейти в папку проекта
cd /home/u/user/domains/einvestor.ru/

# Установить зависимости
composer install --no-dev --optimize-autoloader
```

**⚠️ Если ошибка с правами:**

```bash
# Установить права для текущего пользователя
chown -R $USER:$USER /home/u/user/domains/einvestor.ru/
```

**✅ Проверка:** Должна появиться папка `vendor/` с зависимостями.

---

## ✅ Шаг 5: Настройка .env

```bash
cd /home/u/user/domains/einvestor.ru/

# Скопировать шаблон
cp .env.example .env

# Сгенерировать APP_KEY
php artisan key:generate
```

**Отредактировать `.env`:**

```bash
nano .env  # или другой редактор
```

**Обязательно изменить:**

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://einvestor.ru

# База данных (будем создавать на следующем шаге)
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=ваш_логин_einvestor  # Имя БД из Beget
DB_USERNAME=ваш_логин_einvestor  # Пользователь БД из Beget
DB_PASSWORD=пароль_из_beget       # Пароль из Beget

# Робокасса (продакшн данные!)
ROBOKASSA_MERCHANT_LOGIN=ваш_логин
ROBOKASSA_PASSWORD1=ваш_пароль1
ROBOKASSA_PASSWORD2=ваш_пароль2
ROBOKASSA_HASH_TYPE=md5
ROBOKASSA_IS_TEST=false
```

**Сохранить:** `Ctrl+O` → Enter → `Ctrl+X` (в nano)

---

## ✅ Шаг 6: Создание базы данных в Beget

**Через панель Beget (рекомендуется):**

1. Зайти в панель: https://cp.beget.com
2. Найти **"MySQL-базы данных"** или **"MySQL Databases"**
3. Нажать **"Создать базу данных"** или **"Create Database"**
4. Заполнить:
   - **Имя базы:** `einvestor_prod` (или `ваш_логин_einvestor`)
   - **Пользователь:** создать нового или использовать существующего
   - **Пароль:** указать надежный пароль
5. Записать:
   - **Имя БД:** (например, `логин_einvestor`)
   - **Пользователь:** (например, `логин_einvestor`)
   - **Пароль:** (тот что указали)
   - **Хост:** обычно `localhost`

**✅ Проверка:** База данных создана, данные записаны!

---

## ✅ Шаг 7: Обновить .env с данными БД из Beget

```bash
nano /home/u/user/domains/einvestor.ru/.env
```

**Изменить раздел базы данных на данные из шага 6:**

```env
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=логин_einvestor        # Имя БД из Beget (шаг 6)
DB_USERNAME=логин_einvestor        # Пользователь из Beget (шаг 6)
DB_PASSWORD=пароль_из_beget        # Пароль из Beget (шаг 6)
```

**Сохранить:** `Ctrl+O` → Enter → `Ctrl+X`

---

## ✅ Шаг 8: Применение миграций

```bash
cd /home/u/user/domains/einvestor.ru/

# Проверить подключение к БД
php artisan migrate:status

# Применить миграции (создать таблицы)
php artisan migrate --force
```

**✅ Проверка:** Все миграции применены без ошибок.

---

## ✅ Шаг 9: Импорт данных из WordPress

**Если есть JSON файл с экспортом WordPress:**

```bash
# Загрузить файл на сервер через FileZilla/FTP в папку:
# /home/u/user/domains/einvestor.ru/

# Или через SCP с вашего компьютера:
# scp wordpress-export.json ваш_логин@beget.ru:domains/einvestor.ru/

# На сервере - импортировать
cd /home/u/user/domains/einvestor.ru/
php artisan import:wordpress wordpress-export.json
```

---

## ✅ Шаг 10: Копирование файлов товаров

**Создать папку для товаров:**

```bash
cd /home/u/user/domains/einvestor.ru/
mkdir -p storage/app/products
chmod -R 775 storage/app/products
```

**Скопировать файлы со старого сервера:**

- Через **FileZilla/FTP** - загрузить файлы в `storage/app/products/`
- Или через **SCP** (командная строка)
- Или через **файловый менеджер Beget**

**✅ Проверка:** Файлы товаров есть в `storage/app/products/`.

---

## ✅ Шаг 11: Изменение Document Root в Beget (ВАЖНО!)

**Это ключевой шаг! Нужно изменить корень сайта на `public/` папку Laravel.**

### Через панель Beget:

1. Зайти в панель: https://cp.beget.com
2. Найти **"Домены и поддомены"** или **"Domains"**
3. Найти домен `einvestor.ru`
4. Найти **"Настройки домена"** или **"Domain Settings"**
5. Найти **"Document Root"** или **"Корневая директория"**
6. Изменить на: `/home/u/user/domains/einvestor.ru/public`
   (заменить `u/user` на ваш реальный путь!)
7. **Сохранить**

**⚠️ Важно:** Указывать путь к `public/`, а не к корню Laravel!

**Пример:**
- ❌ Неправильно: `/home/u/user/domains/einvestor.ru/`
- ✅ Правильно: `/home/u/user/domains/einvestor.ru/public`

### Альтернативный способ (если панель не позволяет):

**Если нельзя изменить Document Root через панель, можно:**

1. Скопировать содержимое `public/` в `public_html/`:

```bash
cd /home/u/user/domains/einvestor.ru/
cp -r public/* public_html/
```

2. Обновить пути в `public_html/index.php`:

```php
// Найти в public_html/index.php строки:
require __DIR__.'/../vendor/autoload.php';
$app = require_once __DIR__.'/../bootstrap/app.php';
```

**✅ Проверка:** После изменения Document Root сайт должен открываться!

---

## ✅ Шаг 12: Установка прав доступа

```bash
cd /home/u/user/domains/einvestor.ru/

# Установить права для storage и cache
chmod -R 775 storage bootstrap/cache

# Если нужно - установить владельца
chown -R $USER:$USER storage bootstrap/cache
```

**На Beget обычно права настраиваются автоматически, но лучше проверить.**

---

## ✅ Шаг 13: Оптимизация Laravel для продакшена

```bash
cd /home/u/user/domains/einvestor.ru/

# Очистить кеш
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear

# Создать кеш для продакшена
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

---

## ✅ Шаг 14: Тестирование сайта

**Открыть в браузере:**
- `https://einvestor.ru` - главная страница
- `https://einvestor.ru/products` - товары
- `https://einvestor.ru/articles` - блог
- `https://einvestor.ru/admin` - админ-панель (логин: `admin@einvestor.ru`, пароль: `password`)

**Проверить логи при ошибках:**

```bash
tail -f /home/u/user/domains/einvestor.ru/storage/logs/laravel.log
```

---

## 🔄 Обновление кода в будущем

**Когда нужно обновить код на Beget:**

```bash
# Подключиться по SSH
ssh ваш_логин@beget.ru

# Перейти в папку проекта
cd /home/u/user/domains/einvestor.ru/

# Обновить код из GitHub
git pull origin main

# Обновить зависимости (если composer.json изменился)
composer install --no-dev --optimize-autoloader

# Применить новые миграции (если есть)
php artisan migrate --force

# Очистить и пересоздать кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

---

## 📝 Структура после установки на Beget

```
/home/u/user/domains/einvestor.ru/    <- Laravel проект
├── app/                               
├── bootstrap/                         
├── config/                            
├── database/                          
├── public/                            <- Document Root (в настройках Beget)
│   ├── index.php                     
│   └── ...                           
├── resources/                         
├── routes/                            
├── storage/                           <- Файлы товаров здесь
│   └── app/
│       └── products/                  <- Файлы товаров
├── vendor/                            
├── .env                               <- Конфигурация (защищен!)
└── artisan                            
```

**В настройках домена в Beget указать Document Root:**
```
/home/u/user/domains/einvestor.ru/public
```

---

## ⚠️ Важные замечания для Beget

1. **PHP версия:** В панели Beget можно выбрать версию PHP для домена. Нужна **PHP 8.2+**.

2. **SSL сертификат:** Beget обычно предоставляет бесплатный SSL (Let's Encrypt). Проверить в настройках домена.

3. **Файлы товаров:** Не копировать файлы товаров через Git! Загружать напрямую в `storage/app/products/`.

4. **.env файл:** Файл `.env` должен быть защищен (права 600). На Beget обычно это настроено автоматически.

5. **Логи:** Логи Laravel находятся в `storage/logs/laravel.log`. Проверять при ошибках.

---

## 🆘 Если что-то не работает

**Проблема: Сайт не открывается**

1. Проверить что Document Root указывает на `public/`
2. Проверить права на файлы: `ls -la public/`
3. Проверить логи: `tail -f storage/logs/laravel.log`

**Проблема: Ошибки 500**

1. Проверить `.env` файл - правильно ли указаны данные БД
2. Проверить права на `storage/` и `bootstrap/cache/`
3. Посмотреть логи: `tail -f storage/logs/laravel.log`

**Проблема: Composer не работает**

1. Проверить что Composer доступен: `composer --version`
2. Если нет - обратиться в поддержку Beget

---

**Готово! После выполнения всех шагов Laravel сайт будет работать на Beget! 🚀**

Если возникнут вопросы на каком-то этапе - сообщите!
